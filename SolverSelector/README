
Solver Selecter:

To build the solver selecter, ty


1. Edit Makefile.inc to include the interfaces requested and set the directories. 

2. Set the sqlite3 directory or: 
    a) make sqlite3.build
    b) sudo make sqlite3.install
    c) make sqlite3.clean (optional)

3. Set the waffles directory or:
    a) make waffles.build
    b) make waffles.install
    c) make waffles.clean ( optional) 

3. make
4. make install 

To build the examples, enter the examples directory and type make


